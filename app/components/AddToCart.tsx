"use client";
const AddToCart = () => {
  return (
    <div className="mt-2">
      <button
        className="btn btn-sm btn-accent"
        onClick={() => console.log("Click")}
      >
        Add to card
      </button>
    </div>
  );
};

export default AddToCart;
